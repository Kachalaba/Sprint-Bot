[pytest]
addopts = -q --cov=sprint_bot.application.handlers --cov=sprint_bot.infrastructure.storage.google_sheets --cov-report=term-missing --cov-report=xml
asyncio_mode = auto
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning:aiogram.*
